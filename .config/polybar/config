[bar/bar]
width = 100%
height = 2.5%

foreground = #b9b9b9

module-margin-left = 1
module-margin-right = 1

font-0 = DejaVu Sans
font-1 = Joy Pixels:scale=10

modules-left = i3 music
modules-right = network pulseaudio xkeyboard battery temperature date

tray-maxsize = 6
tray-position = right

separator = " | "

[module/battery]
type = internal/battery

battery = BAT1

format-charging = <label-charging>
format-discharging = <label-discharging>
format-full = <label-full>

label-charging = Charging: %percentage%%
label-discharging = Discharging: %percentage%%
label-full = Fully Charged

[module/date]
type = internal/date

interval = 1.0

date = %Y-%m-%d
time = %H:%M

label = %date% %time%

[module/i3]
type = internal/i3

format = <label-state> <label-mode>

label-focused = %name%
label-focused-background = #5f6d36
label-focused-padding = 2

label-unfocused = %name%
label-unfocused-background = #262b15

label-urgent = %name%

label-visible = %name%

label-separator = |

[module/music]
type = custom/script

exec = fetch-audio-metadata.sh

format = <label>

label = ♫ %output% ♫

[module/network]
type = internal/network

interface = wlp4s0

format-connected = <label-connected>
format-disconnected = <label-disconnected>

label-connected = %essid%, %signal%%, %local_ip% / %local_ip6%
label-connected-foreground = #3ee149

label-disconnected = Not Connected
label-disconnected-foreground = #f00

[module/pulseaudio]
type = internal/pulseaudio

format-volume = <ramp-volume> <label-volume>
format-muted = <label-muted>

label-volume = VOL %percentage%
label-muted = 🔇

ramp-volume-0 = 🔈
ramp-volume-1 = 🔉
ramp-volume-2 = 🔊

[module/temperature]
type = internal/temperature

hwmon-path = /sys/devices/pci0000:00/0000:00:18.3/hwmon/hwmon3/temp1_input

base-temperature = 20
warn-temperature = 70

format = <label>
format-warn = <label-warn>

label = TEMP %temperature-c%
label-warn = TEMP %temperature-c%
label-warn-foreground = #f00

[module/xkeyboard]
type = internal/xkeyboard

label-layout = %name%
